{
    // Minimum sender update period in seconds. (min = 0.025)
    "min_update_period": 0.025,
    // Polled fields update period in seconds. (min = 3.0)
    "polled_fields_update_period": 5.0,
    // Heartbeat period in seconds. (min = 0.1)
    "heartbeat_period": 5000.0,
    // Maximum sender sent rate in MB/s, 0 for no limit.
    "rate_limit_mbs": 0,
    // Array of channels to export (order matters!).
    "channel_names": {
        "poz:v1": { "extra_fields": ["A"] },
        "poz:v2": { "extra_fields": ["A"] },
        "poz:v3": { "extra_fields": ["A"] },
        "poz:v4": { "extra_fields": ["A"] },
        "poz:v5": { "extra_fields": ["A"] },
        "poz:v6": { "extra_fields": ["A"] },
        "poz:v7": { "extra_fields": ["A"] },
        "poz:v8": { "extra_fields": ["A"] },
        "poz:v9": { "extra_fields": ["A"] },
        "poz:v10": { "extra_fields": ["A"] }
    }
}
